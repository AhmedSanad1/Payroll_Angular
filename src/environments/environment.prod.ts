// Live build. The API is hosted at the production server directly.
// Swapped in for environment.ts by the production configuration's
// fileReplacements in angular.json.
export const environment = {
  production: true,
  apiUrl: 'http://216.219.83.248:550/api'
};
